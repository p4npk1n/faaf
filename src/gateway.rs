use crate::config;
use crate::analysis_result;

pub fn analyze(firmware_root_directory: &std::path::Path, analyzer_directory: &std::path::Path, 
    analyzer_config: config::Config, result: analysis_result::AnalysisResult){

}